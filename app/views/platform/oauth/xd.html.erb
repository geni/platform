<!doctype html>
<html>
  <head>
    <title>XD</title>
  </head>
  <body onload="sendFragment()">
    <%= javascript_tag %{
			function sendFragment() {
				(window.opener || window.parent || window.top).postMessage("#{params.to_query}", "#{params[:origin]}");
			}
    } %>
  </body>
</html>